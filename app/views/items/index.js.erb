$('#items').append('<%= j render @items %>');
<% if @items.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate @items %>');
<% else %>
    $(window).off('scroll');
    //$('.pagination').html('NO MORE ITEMS');
    $('.pagination').remove();
<% end %>